{
  "name": "wickra-docs",
  "private": true,
  "type": "module",
  "description": "Wickra documentation site (VitePress, deployed via Cloudflare Pages).",
  "scripts": {
    "dev": "vitepress dev",
    "gen:banner": "node scripts/gen-og-banner.mjs",
    "gen:badges": "node scripts/gen-badges.mjs",
    "build": "node scripts/gen-og-banner.mjs && vitepress build",
    "preview": "vitepress preview",
    "migrate": "node scripts/migrate-wiki.mjs"
  },
  "devDependencies": {
    "@resvg/resvg-js": "^2.6.2",
    "vitepress": "^1.5.0",
    "vue": "^3.5.0"
  },
  "dependencies": {
    "sharp": "^0.33.5"
  }
}
